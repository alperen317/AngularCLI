import { ShowPassWord2Directive } from './show-pass-word2.directive';

describe('ShowPassWord2Directive', () => {
  it('should create an instance', () => {
    const directive = new ShowPassWord2Directive();
    expect(directive).toBeTruthy();
  });
});
